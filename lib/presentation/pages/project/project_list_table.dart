import 'package:flutter/material.dart';
import 'project_tabel.dart';

class ProjectListTable extends StatelessWidget {
  const ProjectListTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const ProjectTable();
  }
}
